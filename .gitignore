# Xcode (project file is generated by xcodegen)
*.xcodeproj/
DerivedData/
build/
.build/
*.profraw

# Swift Package Manager
.swiftpm/
Packages/
Package.resolved

# macOS
.DS_Store

# Editor
.vscode/
.idea/
# sourcekit-lsp build server config (machine-specific DerivedData path)
buildServer.json

# Claude Code working dir + per-session resume scripts
.claude/
session

# Other coding-agent working dirs + skill lockfiles (caveman install, etc.)
.trae/
.qwen/
.agents/
skills-lock.json

# Local DMG build output (scripts/make-dmg.sh)
dist/

# App Store Connect API credentials (Key ID + Issuer ID).
# Never commit — see doc/app-store-submission.md.
.env.appstore

# Gitea Actions runner (internal)
.gitea/
.gitea-runner-config.yaml
.runner
